version: 2.0

jobs:
  "build":
    docker:
      - image: zrlmib/ubuntu-ciitest
        context: zrlmib
    steps:
      - checkout
      - setup_remote_docker
      - run: echo ${DOCKER_LOGIN} && echo $CIRCLE_BRANCH

workflows:
  version: 2
  build:
    jobs:
      - "build"
